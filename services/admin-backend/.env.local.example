ADMIN_TOKEN=local-admin-token
CHECK_MIGRATE=false
# Example commands (only use locally):
# On Windows PowerShell host, to run the existing PowerShell migration helper:
# RUN_MIGRATE_CMD=powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File "..\..\dev\scripts\run-migrations.ps1" -UseLocalAuthStub:$true
# On any host with node installed, to run the local seeder (uses docker exec inside script):
# RUN_SEED_CMD=node ..\..\scripts\run-seed.mjs --local
# Admin allowlist CSV (optional):
ADMIN_ALLOWLIST=127.0.0.1,::1

ADMIN_TOKEN=change-me-locally
CHECK_MIGRATE=false
# Commands to run when migrate/seed endpoints are invoked. Keep these empty to prevent accidental execution.
# Example (local): RUN_MIGRATE_CMD="pwsh -File ../dev/scripts/run-migrations.ps1 -UseLocalAuthStub:$true"
# Example (local): RUN_SEED_CMD="node ../../scripts/run-seed.mjs --local"
RUN_MIGRATE_CMD=
RUN_SEED_CMD=
